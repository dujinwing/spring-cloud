server:
  port: 8080
spring:
  application:
    name: zuul
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: nacos-service
    api-b:
      path: /api-b/**
      serviceId: nacos-service
